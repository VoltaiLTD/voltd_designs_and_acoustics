"use client";import {useRef,useState} from 'react';export default function Visualizer(){const [img,setImg]=useState<string|null>(null);const [color,setColor]=useState('#a0a0a0');const canvasRef=useRef<HTMLCanvasElement>(null);function onUpload(e:React.ChangeEvent<HTMLInputElement>){const f=e.target.files?.[0];if(!f)return;const r=new FileReader();r.onload=()=>setImg(r.result as string);r.readAsDataURL(f);}function render(){if(!img||!canvasRef.current)return;const ctx=canvasRef.current.getContext('2d');if(!ctx)return;const im=new Image();im.onload=()=>{canvasRef.current!.width=im.width;canvasRef.current!.height=im.height;ctx.drawImage(im,0,0);ctx.globalAlpha=.35;ctx.fillStyle=color;ctx.fillRect(im.width*.15,im.height*.25,im.width*.7,im.height*.5);ctx.globalAlpha=1;};im.src=img;}return(<section className='section container'><h1>Volt AI Visualizer</h1><div className='grid md:grid-cols-3 gap-6 mt-4'><div className='card md:col-span-2'><div className='mb-3 flex gap-3 items-center'><input type='file' accept='image/*' onChange={onUpload}/><button className='btn btn-outline' onClick={render}>Render Preview</button></div><canvas ref={canvasRef} className='w-full rounded border border-white/10'/></div><div className='card'><label className='block text-sm text-white/60 mb-1'>Color/Tone</label><input type='color' value={color} onChange={e=>setColor(e.target.value)}/></div></div></section>)}